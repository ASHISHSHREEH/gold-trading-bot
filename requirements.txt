# ── Core ────────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── MetaTrader 5 (Windows only) ──────────────────────────────────────────────
# Install MT5 terminal first: https://www.metatrader5.com/en/download
MetaTrader5>=5.0.45

# ── Configuration ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Visualization / Reporting ────────────────────────────────────────────────
matplotlib>=3.7.0
plotly>=5.14.0

# ── Removed: yfinance, requests, psycopg2, SQLAlchemy, google-generativeai ──
# All data now comes directly from MT5 terminal.
# SQLite (built-in) is used for analytics — no external DB needed.
