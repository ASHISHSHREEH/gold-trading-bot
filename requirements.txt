# Core
pandas>=2.0.0
numpy>=1.24.0

# MT5 execution engine (Windows only — requires MT5 terminal installed)
MetaTrader5>=5.0.45

# Configuration
python-dotenv>=1.0.0

# Visualisation (optional — for charting / backtesting analysis)
matplotlib>=3.7.0
